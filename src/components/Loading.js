
import React from "react";
import { Center, Ring, Text } from "../styles/LoadingElements";

const Loading = () => {
  return (
    <Center>
      <Ring />
      <Text>Loading...</Text>
    </Center>
  );
};

export default Loading;